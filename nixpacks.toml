# Railway Nixpacks konfiguratsiyasi (Docker'siz).
# Node versiyasi .nvmrc (20) va package.json "engines" orqali aniqlanadi.
# Bu yerda faqat tizim FFmpeg + fontconfig qo'shamiz.

[phases.setup]
aptPkgs = ["ffmpeg", "fontconfig"]

[variables]
FFMPEG_PATH = "/usr/bin/ffmpeg"
NODE_ENV = "production"
